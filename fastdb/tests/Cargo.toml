[package]
name = "fastdb-tests"
version = "1.0.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
fastdb = { path = "../frontend" }
fastql-parser = { path = "../parser" }
tempfile.workspace = true
turso_core = { path = "../../core" }
