--- a/fastdb/frontend/Cargo.toml
+++ b/fastdb/frontend/Cargo.toml
@@ -28,6 +28,7 @@
 
 # The pinned QuickJS release has no pregenerated threaded-WASI bindings.
 [target.'cfg(all(target_arch = "wasm32", target_os = "wasi"))'.dependencies]
+turso_core = { path = "../../core", features = ["fts_wasi"] }
 rquickjs-sys = { version = "=0.12.2", features = ["bindgen"] }
 
 [[example]]
