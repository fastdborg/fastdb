# FastDB / FastQL 2.1 production checklist

User-approved objective: production release for V2, version 2.1.0. Native Linux
x64 only, seven implemented clients. No cloud, browser/WASM or graph database
scope. Preserve SQL joins, typed records, bounded document references and inverse
lookups. Released 2.0.0 and its artifacts remain immutable.

- [x] P0 implementation: source-preserving SQLite snapshot/adoption CLI,
  supported-schema matrix, four permanent regressions and 14 stock-SQLite
  fixture groups; repeat the harness on the exact final artifact.
- [x] P1 implementation: permanent sync/async Node process-ownership tests and
  Linux single-owner deployment contract; qualify again on final artifacts.
- [ ] P2: optimized native shipping profile retaining assertions/overflow checks,
  artifact provenance, installed seven-client and standalone Rust verification,
  application workload measurements and supported resource envelope.
- [ ] P3: FTS/ANN corpus, dimensions and connection limits based on measured
  artifacts; document whole-corpus/native-call costs and operating headroom.
- [x] P4 implementation: native returned ENOSPC/EIO/short-write/fsync errors at
  commit/checkpoint; old/new atomic state, V2 indexes, subsequent writes and two
  reopens pass. See [fault evidence](native-io-errors-evidence.md).
- [x] P4 correction: qualify and separately review upstream's NORMAL checkpoint
  WAL-sync backport. The upstream crash model reproduces integrity failure on
  the pinned baseline; the FULL-mode control passes. The isolated correction
  passes seven focused regressions and the 126 checkpoint / 38 VACUUM upstream
  filters. The user approved the exact patch; it is integrated with seven
  permanent regressions passing. See [review and maintenance criteria](proposals/checkpoint-wal-sync.md).
  Combined-source and final artifact checks remain separate release gates.
- [x] P5 implementation: operations/backup/recovery runbook, scoped dependency
  and bundled-C advisory review, enabled private security reporting and core
  exception maintenance ownership. The current shipping lockfile has no
  unresolved findings in the pinned review; package the receipts with the final
  artifact and recheck if dependencies change.
- [x] Integrate/review nested projections PR #8, including compact bracket-identifier
  compatibility. Hosted CI run 36142579141 passes; merged as `6e8005e8dfe6780981dcb8dd08483004f051dab1`.
- [x] Align version 2.1.0 and regenerate complete notices. Preparation checks:
  750 Rust, 121 Node and three C ABI tests, fmt/Clippy/TypeScript pass; see
  [verification](verification.md). Repeat affected checks after core integration.
- [ ] Complete final source review and hosted scoped CI.
- [ ] Build clean candidate, qualify all exact artifacts and upgrade/restore.
- [ ] Publish versioned release; download and independently verify checksums.

Each completed entry records its exact source/artifact evidence. Implementation
checks do not imply final artifact qualification. Online backup, registry
publication, new platforms, graph APIs and driver-standard adapters are outside
this finite release gate.
